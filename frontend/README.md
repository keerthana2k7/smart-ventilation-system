# Smart Restroom Ventilation – Frontend

- React + Vite + Tailwind + Framer Motion + Recharts
- Dark theme with red accent. Place your logo at `public/logo.png` and favicon at `public/favicon.ico`.

## Scripts

- `npm run dev`
- `npm run build`
- `npm run preview`

Set `VITE_API_URL` in `.env` (e.g. `http://localhost:5000`).


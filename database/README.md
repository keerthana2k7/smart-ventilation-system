# Database

MySQL is initialized automatically by the backend on startup. It will create the database `smart_ventilation_system` and required tables if they do not exist:

- `users`
- `sensor_data`
- `uploads`

Optional: `schema.sql` contains the schema for manual setup.

